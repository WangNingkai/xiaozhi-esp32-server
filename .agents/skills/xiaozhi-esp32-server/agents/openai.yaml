interface:
  display_name: "Xiaozhi Esp32 Server"
  short_description: "Repo-specific patterns and workflows for xiaozhi-esp32-server"
  default_prompt: "Use the xiaozhi-esp32-server repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true